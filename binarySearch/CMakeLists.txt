CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

SET(CMAKE_CXX_STANDARD 11)

PROJECT(BinarySearch)

INCLUDE_DIRECTORIES(include)

file(GLOB SOURCES "src/*.cpp")

ADD_LIBRARY(main "catch.hpp")

ADD_EXECUTABLE(main ${SOURCES})
